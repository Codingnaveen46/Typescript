// ternary / conditional operator 

// ? : 

// first we will check if the condition is true, if it is then we will return the first value, otherwise we will return the second value

//syntax expression ? valueIfTrue : valueIfFalse
/* 
let a: number = 10 , b:number = 20;

let res:number = (a > b) ? a : b;

console.log(res); // 20 */

//example 2 

let personage : number = 30;
let result: string = (personage >= 18) ? "Adult" : "Minor";
console.log(result); // Adult